pipeline {
    agent any

    stages {
//         stage('Test') {
//             steps {
//                 sh './gradlew test'
//             }
//         }

        stage('Build FatJar') {
            steps {
                // Use shadowJar task to build the FatJar
                sh './gradlew clean'
                sh './gradlew :server:shadowJar'
            }
        }

        stage('Build Docker Image') {
            steps {
                // Copy the built jar to root so Docker context can see it
                sh 'cp server/build/libs/server-all.jar /opt/docker-manager/server-all.jar'
            }
        }

       stage('Deploy') {
           environment {
               BUILD_NUMBER = "${env.BUILD_NUMBER}"
           }
           steps {
               sh 'cp docker-manager.service /opt/docker-manager/docker-manager.service'
               sh '/jenkins/bin/jar-deploy.sh /jenkins/deploy/server-all.jar'
            }
        }
    }
    
    post {
        always {
            cleanWs()
        }
    }
}
